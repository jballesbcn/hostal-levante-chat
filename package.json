
{
  "name": "hostal-levante-chat",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild main.js --bundle --outfile=dist/main.js --define:process.env.API_KEY=\"\\\"$API_KEY\\\"\" --minify",
    "start": "serve ."
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
